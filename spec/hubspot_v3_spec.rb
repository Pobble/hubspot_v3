# frozen_string_literal: true

RSpec.describe HubspotV3 do
  it "has a version number" do
    expect(HubspotV3::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
